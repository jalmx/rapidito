import React from "react";

const Contact = ({ title }) => (
  <section>
    <h1 className="titleSection">{title}</h1>
    <article className={""}>cocntato</article>
  </section>
);

export default Contact;
